const headerTitle = {
  fontWeight: 600,
  fontSize: 16,
} as React.CSSProperties;
const cardTool = { width: '100%', height: '90%' } as React.CSSProperties;
const cardBarSearch = { display: 'flex', gap: '10px' } as React.CSSProperties;

export default {
  headerTitle,
  cardTool,
  cardBarSearch,
};
