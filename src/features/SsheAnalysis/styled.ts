const headerTitle: React.CSSProperties = {
  fontWeight: 600,
  fontSize: 16,
};

const cardTool: React.CSSProperties = { width: '100%', height: '90%' };

export default {
  headerTitle,
  cardTool,
};
